'use client';

import { JSX } from 'react';
import GoldApplyButton from './gold-apply-button';

/** Hero primary CTA — opens gold loan application */
const GoldHeroCta = (): JSX.Element => {
  return <GoldApplyButton />;
};

export default GoldHeroCta;
