/** Shared shape for lavender feature cards and prose tip rows */
export interface ProseCardItem {
  id: string;
  title: string;
  description: string;
}

export interface InsightCalloutContent {
  title: string;
  description: string;
}
